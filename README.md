# 2027 冬遊

Trip itinerary page for the crew. Plain HTML/CSS/JS, no build step.

## Edit the itinerary

Edit the `days` array in `script.js` — each entry becomes a day card.

## Preview locally

Open `index.html` directly in a browser, or run a local server:

```bash
python3 -m http.server 8000
```
